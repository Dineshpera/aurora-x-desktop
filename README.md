# AURORA-X Desktop (v0.1 MVP)
AI-powered, offline-first video editor for creators — free (₹0).
This repo contains the Electron + Rust code for the AURORA-X Desktop beta.

## Getting started (dev)
1. Install Node (16+), Rust, FFmpeg
2. Clone repo: git clone ...
3. npm install
4. npm run dev
